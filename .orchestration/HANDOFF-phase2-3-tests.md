# Handoff: Phase 2 + Phase 3 web-modern test coverage

## Bottom line

| Metric                | Pre-phase | Post-phase | Delta     |
|-----------------------|----------:|-----------:|----------:|
| Test files            | 5         | 22         | **+17**   |
| Passing tests         | 72        | **316**    | **+244**  |
| Tested modules @ 100% | 4 of N    | 12 of N    | +8        |

`npm --prefix web-modern test` → **316 / 316 green** in 4.0s.
`npm --prefix web-modern run typecheck` → **clean** (no diagnostics).

## Phase 2: inventory tests (2 workers, 2 branches)

| Worker             | Branch                                  | Tag                          | Tests added |
|--------------------|-----------------------------------------|------------------------------|------------:|
| lib-inventory      | `wip/phase2-inventory-lib-inventory`    | `phase2-inventory-lib-v1`    | 5 (coverage)|
| routes-inventory   | `wip/phase2-inventory-routes-inventory` | `phase2-inventory-routes-v1` | 37 (route)  |

- **`web-modern/src/lib/inventory/status.ts`** — closed 3 untaken branches
  (lines 51, 74, 89). Now 100% on every coverage dimension.
- **`web-modern/src/routes/app/inventory/{index,$itemId}.test.tsx`** —
  first route-level tests in web-modern. Established the TanStack Router
  + Query mock pattern (pass-through Link, mocked `getJson`/`postJson`,
  fresh `QueryClient` per test with `retry: false`).

## Phase 3: web-modern test surface (4 workers, 4 branches)

| Worker            | Branch                                     | Tag                              | Tests added |
|-------------------|--------------------------------------------|----------------------------------|------------:|
| lib-api-edge      | `wip/phase3-web-modern-lib-api-edge`       | `phase3-lib-api-edge-v1`         | 36          |
| lib-utils-rbac    | `wip/phase3-web-modern-lib-utils-rbac`     | `phase3-lib-utils-rbac-v1`       | 58          |
| components-shell  | `wip/phase3-web-modern-components-shell`   | `phase3-components-shell-v1`     | 59          |
| routes-crm        | `wip/phase3-web-modern-routes-crm`         | `phase3-routes-crm-v1`           | 49          |

Coverage on the targeted modules:

| Module                                | Stmts  | Branch | Funcs  | Lines  |
|---------------------------------------|-------:|-------:|-------:|-------:|
| `src/lib` (root)                      | 100%   | 100%   | 100%   | 100%   |
| `src/lib/agents`                      |  94.0% |  74.5% |  71.4% |  94.0% |
| `src/lib/api`                         |  86.9% | 100%   | 100%   |  86.9% |
| `src/lib/inventory`                   | 100%   | 100%   | 100%   | 100%   |
| `src/lib/rbac`                        | 100%   | 100%   | 100%   | 100%   |
| `src/lib/utils`                       | 100%   | 100%   | 100%   | 100%   |
| `src/components/ui`                   | 100%   | 100%   | 100%   | 100%   |

(All targeted modules exceed the 80% bar. The all-files total is
52.78% because route shells — `copilot.tsx`, `desk/*`, `index.tsx` —
still need a TanStack Router harness. The two route groups that
**were** tested, `app/crm/*` and `app/inventory/*`, sit at 96% lines.)

## Merge order executed

```
8294e65  merge: wip/phase2-inventory-routes-inventory
9966c96  Merge ant/wip/phase3-web-modern-lib-api-edge
9257c97  Merge ant/wip/phase3-web-modern-lib-utils-rbac
51e35af  Merge ant/wip/phase3-web-modern-components-shell
553c532  Merge ant/wip/phase3-web-modern-routes-crm
6acae58  chore(routes): regenerate routeTree.gen.ts
```

All 5 phase-2/3 worker branches landed on `main` via `git merge --no-ff`
from `ant/wip/phase3-*`. Conflicts were uniformly two: an `add/add`
`package-lock.json` (regenerated via `npm install --legacy-peer-deps`)
and a one-line `package.json` block where each branch added
`@testing-library/dom` or `@vitest/coverage-v8` independently — both
kept. `web-modern/src/lib/inventory/__tests__/status.test.ts` had a
similar add/add pattern from the pre-merge state; HEAD's content
(lib-inventory's branch-coverage tests) was preserved verbatim.

## Push to `ant`

`main` is 5 commits ahead of `ant/main` and ready to push. **Awaiting
explicit user approval** before running `git push ant main`.

## Constraints honored

- No source files modified — every worker was test-only.
- No `apps.ts` entries added or removed (the 13-apps list stays at 13,
  preserving `test/api.test.js:84`).
- No `git push` performed (only `git push -u ant <branch>` from inside
  the worktrees by the workers themselves, against their own wip
  branches — per the plan's hard constraint).
- No `origin` push — `origin` was never touched.
- `routeTree.gen.ts` is committed; it is auto-regenerated by
  `tsr generate` (postinstall).
- Armenian-first UI labels: not applicable — no UI strings live in
  the modules under test.
