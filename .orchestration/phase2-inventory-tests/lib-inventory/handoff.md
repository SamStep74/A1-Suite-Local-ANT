# Handoff: lib-inventory

## Summary
- Pushed `web-modern/src/lib/inventory/` to **100% line AND 100% branch coverage**
  by closing the 3 untaken branches in `web-modern/src/lib/inventory/status.ts`.
- Branch `wip/phase2-inventory-lib-inventory` pushed to `ant`.
- Tag `phase2-inventory-lib-v1` pushed to `ant`.

## Branches Closed
| Source line | Function | Branch closed |
|---|---|---|
| `status.ts:51` | `summariseItemStock` | `asNum(r.quantity) ?? 0` — second operand of the nullish chain (both operands non-finite) |
| `status.ts:74` | `indexByCatalogItemId` | `it.stockBalances ?? []` — fallback when `stockBalances` is null/undefined |
| `status.ts:89` | `totalStockByItemId` | `asNum(b.quantity) ?? 0` — second operand of the nullish chain (both operands non-finite) |

The first operand of the `??` chains (51, 89) was already covered by the
seeded tests; the second operand needed new tests. The first operand of
the `??` chain at line 74 was also already covered by the existing
`stockBalances: []` test, but the take-right branch (null/undefined)
needed a new test.

## Tests Added (5 new, 19 total in file)
All added to the existing `web-modern/src/lib/inventory/__tests__/status.test.ts`:

1. `summariseItemStock > falls back to quantity when availableQuantity is non-finite`
   — exercises the `asNum(r.quantity)` branch when `asNum(r.availableQuantity)`
   returns null.
2. `summariseItemStock > treats rows with no usable quantity as zero` (closes :51)
   — both `availableQuantity` and `quantity` are NaN; verifies total is 0
   (not NaN).
3. `indexByCatalogItemId > treats a null stockBalances as no balances` (closes :74)
   — covers both `null` and `undefined` cases.
4. `totalStockByItemId > falls back to quantity when availableQuantity is non-finite`
   — same shape as (1) but for `StockBalance[]`.
5. `totalStockByItemId > treats balances with no usable quantity as zero` (closes :89)
   — both `availableQuantity` and `quantity` are NaN.

## Final Coverage for `src/lib/inventory/`
| Metric      | Before | After |
|-------------|--------|-------|
| Lines       | 100%   | **100%** |
| Branches    | 83.87% (3 untaken) | **100%** (0 untaken) |
| Functions   | 100%   | **100%** |
| Statements  | 100%   | **100%** |

## Other Files Committed
- `web-modern/src/lib/inventory/status.ts` (untracked, seeded — picked up by `git add -A`)
- `web-modern/src/routes/app/inventory/{index,$itemId}.tsx` (untracked, seeded)
- `web-modern/src/routeTree.gen.ts` (regenerated by `tsr generate` postinstall)
- `web-modern/package.json` / `web-modern/package-lock.json` — see "Notes" below

## Tests / Verification
- `npx vitest run src/lib/inventory/__tests__/status.test.ts` — 19/19 pass
- `npx vitest run` — 72/72 tests pass (the seeded suite); the only failure is
  `HybridBadge.test.tsx` which is a pre-existing breakage in the seed overlay
  (missing `@testing-library/dom` and an unexported `screen` from
  `@testing-library/react`). **Not caused by this worker's changes.**
- `npx tsc --noEmit` — clean except for the same pre-existing
  `HybridBadge.test.tsx` error. **Not caused by this worker's changes.**
- `npx vitest run --coverage src/lib/inventory/` — 100/100/100/100 for the
  inventory subtree.

## Follow-ups / Notes
- **`@vitest/coverage-v8` version pin**: the seeded `package.json` declared
  `^4.1.8`, but the project pins `vitest@^3.0.0` (resolved to `3.2.6`).
  v4 removed the `BaseCoverageProvider` export that v3 expects, so
  `npx vitest run --coverage` failed with a SyntaxError on first try.
  I downgraded to `^3.2.6` in a separate `chore` commit.
- **No source changes**: per the task rules, I did not modify `status.ts`.
  The 3 untaken branches were all defensive nullish-coalescing fall-throughs
  that the existing tests didn't exercise; my 5 new tests close them
  without changing any production logic.
- **No bugs found** in the source during testing — the 3 untaken branches
  are defensive guards, not bugs.
- **`HybridBadge.test.tsx` is broken in the seed overlay** — out of scope
  for this worker, but worth flagging for the next phase.
